{
  "name": "5chairs-web-onboarding",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "start:backend": "python -m uvicorn backend.main:app --reload --host 0.0.0.0 --port 8000"
  },
  "dependencies": {
    
  },
  "devDependencies": {
    "terser": "^5.44.0",
    "vite": "^5.0.0"
  }
}
